{
  "name": "folio",
  "version": "0.3.20",
  "type": "module",
  "scripts": {
    "dev": "vite src --open",
    "build": "node build.js",
    "preview": "node -e \"const{exec}=require('child_process');exec('start dist/folio.html')\""
  },
  "dependencies": {
    "@cantoo/pdf-lib": "^2.6.5",
    "pdfjs-dist": "^5.6.205"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "vite": "^5.0.0"
  }
}
